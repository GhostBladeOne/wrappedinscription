# Wrapped BEP20 Contract

This project demonstrates a wrapped BEP20 inscription use case.

Try running some of the following tasks:

```shell
npx hardhat help
npx hardhat test
REPORT_GAS=true npx hardhat test
npx hardhat node
npx hardhat run scripts/deploy.ts
```
